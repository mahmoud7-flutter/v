import 'package:flutter/material.dart';

class Mmoohh extends StatelessWidget {
  const Mmoohh({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
       // backgroundColor: Colors.red,
        title: Text("mahmoud   ghabsha"),
        
      ),
      body: Center(),
    );
  }
}

