import 'package:flutter/material.dart';

class mahmoud extends StatelessWidget {
  const mahmoud({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("دليل سياحي"),
      ),
    );
  }
}
